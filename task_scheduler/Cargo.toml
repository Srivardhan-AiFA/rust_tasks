[package]
name = "task_scheduler"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"

reqwest = "0.13.1"

tokio = { version = "1", features = ["full"] } 

sha2 = "0.10"
hex = "0.4" # Optional: for formatting the output as a hexadecimal string